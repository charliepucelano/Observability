# Recent Corrective Fixes (Do Not Re-diagnose)

This is a running changelog of fixes and mitigations applied to the homelab. When analyzing logs and metrics, please assume these issues are already resolved and do not propose them as new solutions unless the metrics show the mitigation has explicitly failed.

- **[2026-06-12] WSL Memory Limit**: Applied a strict 16GB memory limit in `.wslconfig` to prevent `vmmemWSL` (Docker Desktop) from exhausting the host's 128GB of RAM.
- **[2026-06-13] Process Monitor Integration**: Deployed `process-monitor.ps1` as a background service to sample Top CPU/RAM processes every 60s. Promtail now ingests this into Loki (`{job="windows-processes"}`) for historical correlation.
- **[2026-06-13] Immich Thumbnail Loop Fix**: Quarantined 485 corrupt image files (bad headers) from `/data/library/admin/Camera Roll/` that were causing the Immich thumbnail generation microservices to infinitely loop, resulting in 138,000+ errors in Loki and high system load.
- **[2026-06-14] DevOps Analyzer Chunking**: Fixed devops_analyzer script causing Chrome CDP timeouts and dropping content by improving the JS chunking logic.
- **[2026-06-14] OpenClaw CLI Cleanup**: Removed stale `repo-openclaw-cli-1` container that exited 5 weeks ago.
- **[2026-06-14] Blackbox Exporter Redirects**: Updated prometheus.yml targets to their final redirect destinations (e.g., adding `/login` or `/app/`) to eliminate noisy HTTP 301/302 warnings in the blackbox exporter logs.
- **[2026-06-14] Universal Interactive Execution**: Refactored `telegram-bot-listener.ps1` to intercept `<execute>` tags from `/ask`, `loki_triage.py`, and `silent_error_hunter.py`, attaching an inline Telegram keyboard (Approve/Reject) for secure one-touch local remediation.
- **[2026-06-14] Implemented /ask Chat Command**: User wanted a natural language way to query homelab status. Created ask_homelab.py that uses ChromaDB RAG and local LM Studio (qwen/qwen3-30b-a3b-2507) to answer user queries from Telegram. Added /ask switch to telegram-bot-listener.ps1.
- **[2026-06-15] Telegram Listener Syntax Fix**: Fixed a syntax error (extra `}`) in `telegram-bot-listener.ps1` that was causing the GeekomBot service to fail on startup. Validated that Telegram HTML parsing and Auto-Remediation workflows work flawlessly.
- **[2026-06-18] Cloudflared Route for PDF Service**: Added hostname mapping for `pdf.leyforge.com` pointing to `http://localhost:80` in cloudflared config.yml.
- **[2026-06-18] Observability Uptime Monitor Update**: Added `https://pdf.leyforge.com` to Prometheus blackbox uptime monitoring targets.
- **[2026-06-18] GeekomBot Uptime & Logs Update**: Added `stirling-pdf` and `open-webui` to GeekomBot's aliases, uptime URL list, logs command, and restarted the `GeekomBot` service.
- **[2026-06-18] Stirling PDF Config Update**: Updated `docker-compose.yml` to use `frooodles/pdf:latest`, modified bind mount from `/data` to `/configs`, and added localization environment settings.
- **[2026-06-21] devops_analyzer Skill Deletion & Auto-Restore**: The `devops_analyzer` OpenClaw skill was wiped from the live workspace (`D:\openclaw\data\skills\`) during a container rebuild. Restored from `D:\backups\openclaw-snapshot\`. Added an auto-restore safeguard to `weekly-analysis.ps1` that checks for the skill before running and restores it from the backup snapshot if missing.
- **2026-06-21**: Removed cs collector from windows_exporter config to fix crash loop.
- **2026-06-21**: Ran docker system prune --volumes -f to reclaim disk space.
- **2026-06-21**: Filtered ImproperImageHeader logs from immich_server in Promtail.
- **2026-06-21**: Updated  nalyze.sh watchdog to support new Perplexity output classes.
- **[2026-06-23] Fixed Vite 5 `allowedHosts` connectivity issue for `camperplan` and `viaje` by removing corrupted spaces in `VITE_APP_DOMAIN` inside their respective `.env` files.
- **[2026-06-23] Bypassed Nginx Proxy Manager for Stirling PDF (`pdf.leyforge.com`), mapping Cloudflare Tunnel directly to the local port to resolve proxy timeout loops.
- **[2026-06-27] RoadTripPlanner Firebase Hooks & Bash Scripts**: Fixed race conditions during atomic list reordering by adding an `updateStops` hook in Firebase. Replaced broken inline heredocs in `crawl_one.sh` with standalone `.js` scripts to fix module errors.
- **[2026-06-27] CamperPlan Bug Fixes & Hardening**: Fixed "calculating..." bug by properly calculating and persisting legs. Implemented atomic reordering for TripPoints. Updated agent scripts to use `.env.agent` instead of hardcoded Firebase URLs and API keys. Removed legacy Perplexity code. Cleaned up Maps libraries and `Navigation.tsx`. Added `ErrorBoundary.tsx` and paginated campsite search results for better UI performance.
- **[2026-06-27]**: Fixed camperplan RouteMap, TripSetup disjointed state loops, AdminPanel missing function crash, and Typescript errors.
- **[2026-06-28] Observability Enhancements Implementation**: Implemented a comprehensive observability enhancement roadmap including Grafana native Telegram alerting, backup verification scripts, dashboard auto-provisioning, Network/ISP monitoring with speedtest exporter, SLO tracking, container auto-restart watchdog, RAG auto-reindexing, VictoriaMetrics long-term storage, and Playwright synthetic user journeys.
- **[2026-06-28] Grafana Telegram Alerting Fix**: Hardcoded `chatID` in `contact-points.yaml` to bypass a Grafana provisioning bug where purely numeric environment variables (like Telegram Chat IDs) are auto-converted to `float64`, causing JSON unmarshal crashes when the Go struct strictly expects a string.
- **[2026-06-28] Container Watchdog Service Fix**: Wrapped the logic in `container-watchdog.ps1` in an infinite `while ($true)` loop so NSSM correctly identifies it as a continuous service instead of immediately pausing it (`SERVICE_PAUSED`) upon exit.
- **[2026-06-28] Weekly Analysis Double-Trigger Fix**: Replaced in-memory `$lastWeeklyAnalysis` guard in `telegram-bot-listener.ps1` with a persistent file-based lock (`D:\scripts\.weekly-analysis-lastrun.txt`). The in-memory variable was reset whenever NSSM restarted the GeekomBot service, allowing the Sunday 09:00 trigger to fire again.
- **[2026-06-28] Weekly Analysis False-Failure Fix**: Changed `weekly-analysis.ps1` to check for the existence of `latest-analysis.txt` as the success signal instead of solely relying on the exit code from `analyze.sh`. The `set -euo pipefail` in the bash script caused non-zero exits from the cleanup `node` heredoc even when Perplexity had already been successfully queried. Also guarded the heredoc with `|| true`.
- **[2026-06-28] Critical C: Drive Space Mitigation**: Reclaimed over 300GB of space by deleting 88GB of stale VMs on Desktop, 8.5GB of Downloads, running a full Docker image prune (-11.7GB), and offloading a massive 315GB Macrium `.mrimgx` system backup to the F: drive, resolving the Sunday Weekly Analysis C: drive alert.
- **[2026-06-30] Stirling PDF Healthcheck Fix**: Fixed Stirling PDF Docker healthcheck by removing `-f` from the `curl` command. The server was returning 401 Unauthorized for the health check which caused the container to be incorrectly marked as unhealthy and restarted by autoheal, triggering Grafana uptime alerts.- **[2026-06-30] Uptime Monitor Loopback Fix**: Updated both prometheus.yml (Blackbox Exporter targets) and telegram-bot-listener.ps1 to probe `http://host.docker.internal:<port>` or `http://localhost:<port>` instead of public `https://*.leyforge.com` URLs. This bypasses the local ISP router (Telefonica) hijacking DNS requests and incorrectly resolving them to the local proxy (which was timing out since Nginx is no longer handling these domains).
- **[2026-06-30] Uptime Monitor Loopback Revert**: Reverted the previous loopback probe config in prometheus.yml because altering the relabeling rules caused Blackbox Exporter to improperly format headers/routes resulting in massive timeout alerts across all services. Excluded pdf.leyforge.com from Blackbox Exporter targets entirely per user request.
- **[2026-07-04] Prometheus Uptime Target Relabeling**: Replaced static host.docker.internal targets with public leyforge.com URLs in prometheus.yml static_configs, and added a relabel_configs mapping to rewrite the __param_target to the local host.docker.internal:<port> URL. This routes the probe correctly over the local network (bypassing the Cloudflare Tunnel/Router loopback) while preserving the human-readable public domain in the 'instance' label for Grafana and Telegram alerts.
- **[2026-07-04] Alerting Triage**: Removed the legacy `blackbox_service_down` alert rule from Grafana's provisioned `alerts.yaml` to silence 'ghost' MissingSeries alerts caused by target relabeling. We now rely exclusively on the new SLO-based error budget alerts.
